package com.helix.common.ingest;

/** External source systems Helix ingests from (PRD §8 integrations). */
public enum SourceSystem {
    CREDIT_BUREAU,
    CORPORATE_REGISTRY,
    GST_TAX,
    SANCTIONS_SCREENING,
    CORE_BANKING,
    MARKET_DATA,
    DOCUMENT_STORE,
    COLLATERAL_REGISTRY
}
