server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: portfolio-service
  datasource:
    url: jdbc:sqlite:${HELIX_DATA_DIR:./data}/portfolio.db
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

helix:
  config-service:
    base-url: ${CONFIG_SERVICE_URL:http://localhost:8081}
  origination-service:
    base-url: ${ORIGINATION_SERVICE_URL:http://localhost:8083}
  risk-service:
    base-url: ${RISK_SERVICE_URL:http://localhost:8084}
  decision-service:
    base-url: ${DECISION_SERVICE_URL:http://localhost:8085}

